########################################
    exp12 task
    Release:    2023.10.21, v1
    Update:     2023.10.27
########################################
[TODO]:
    1. 增加cssrd, csrwr, csrxchg, ertn指令
    2. 增加syscall指令
[HINT]:
    1. csr.v模块打包了csr的一些操作，但是不保证正确性 :D
    2. 按照讲义，在WB级完成中断和异常的处理
    3. 流水级缓存总线还需要改！
    4. CSR寄存器可能存在数据冲突，按讲义此处使用阻塞解决
    5. 暂时只有这么多。想到了再写。

2023.10.27
[HINT]:
    1. 分支priv-lab5（目前）为yzcc通过仿真的exp12，仅供参考
    2. 本人在调的时候遇到的一些问题，大家写的时候可以多关注：
        1） 中断/例外处理时（wb_ex拉高），DHD模块能否发出pause信号？
            * 下面中断/例外统称例外 :P *        
        2） 多拍的流水级和例外之间应该如何处理？如EX级除法多拍，此时WB级给出例外信号会发生什么？
            * 实际上1）、2）本质时一样的，都是wb_ex信号只能维持一拍。本人的解决方法是把上述操作在例外到来时都变成一拍。 *
        3） 在pre-IF级初次选pcnext时候，可能br_target、wb_ex、ertn_flush均为X（因为各流水级还没更新过），如何解决？
            * 考虑与valid结合，valid有复位 :D *
        4） 例外到来时（wb_ex拉高），分支指令br_taken能否拉高？
    3. 暂时这么多，想到了再写。